# Phase 8 · 收尾对接 + 退役清单

> 目标：剩余视图逐个接真、摘净 DemoTag、清理过渡桥、全量回归。
> 依赖：Phase 2–7 全部完成。

## 改动 1 · 剩余视图接真（每接通一个，删该视图的 `WsDemoTag`）

| 视图 | 后端 | 注意 |
|---|---|---|
| 风格 styleref | `/api/v2/style-reference`（ingest→extract→synthesize→inject→validate） | 「应用到本项目」已在 P5 走决策卡；风格库**跨作品共享**（原型注释明确 wsKey 不隔离它） |
| AI 起草台 scene | `scenes.py` + `chapters/{id}/run/full` + `run-status` | 队列/运行态轮询沿用既有 run-status |
| 成稿中心 manuscripts | `chapter_manuscripts.py` | 归档动作已接 P7 写回链 |
| 章节编排 author | 目录 API（P3）+ `author_desk.py` | loom/pacing/doctor 三个子面板若需后端数据，列差集再决定（允许先留演示标签） |
| 发布索引 index | `indexing.py` | |
| 导入导出 interop | `interop.py` | |
| 设置 settings | `system_config.py` + LLM 节点注册表 | UI 偏好（主题/动效/版式）永久留 localStorage |
| 深改面板（写作内） | `writer_deep_review.py` | 核对采纳/忽略/重扫/撤销四动作；缺 undo 则在服务里补「最近一次采纳的逆操作」（仅一步） |

## 改动 2 · 过渡设施清理

- 删除全部 `window.*` 兼容赋值（Phase 1 留下的）；删除已迁移完的 localStorage 旧键读取分支与迁移代码（保留打过的迁移标记判断，避免重复迁移）。
- `wsKey()` 仅剩 UI 偏好使用者；grep 确认无业务数据再过它。
- ⌘K 数据源确认 = 目录 API + 雪花步骤 + 固定动作表（纯客户端模糊匹配，无后端搜索）。

## 改动 3 · 退役评估（⚖️ D6 默认：暂不删 Vue）

- `start-dev.cmd` 默认打开 React 前端（5174）；Vue（5173）降为备用，README 标注。
- 把 Vue 端 Playwright E2E 里**与后端契约相关**的用例平移到 React 端（`frontend-react/tests/`）；纯 Vue 组件测试不平移。
- 在仓库 CLAUDE.md 增补 React 前端段落（目录结构、store 缝合面契约的位置、dev 命令）。

## 全局验收（出包前最后跑）

1. 断网刷新 React 前端：业务数据零 localStorage 依赖（DevTools 检查）。
2. 全链路冒烟：新建空白书 → 雪花十步 → 物化 → 编排 → 起草 → 写作 → 深改 → 归档 → 成稿；期间产生的每张待办卡可处理且 effect 落库。
3. 删整部书 → 回收站整体恢复无损。
4. 派生项三语义（不可划掉/自动消失/指纹复浮）抽查。
5. `python -m pytest`、`scripts/verify_windows.ps1`、WSL strict、`npm run build`（两个前端）、Playwright 全绿；`alembic heads` 单头。
6. 全仓 grep `WsDemoTag`：只允许出现在确实未接真的视图（应为零或有明确 PROGRESS.md 记录的例外）。

提交：`FE-ALIGN Phase 8: cutover`。
