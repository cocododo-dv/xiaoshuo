"""Frozen application-level defaults shared by accounting schema and runtime config."""

DEFAULT_PROVIDER_ATTEMPT_BUDGET = 32
PROVIDER_ATTEMPT_BUDGET_CONFIG_KEY = "retry_budget.provider_attempt_budget"
